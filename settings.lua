return {
    colors = {
        std = colors.white,
        bg = colors.black,
        light = colors.gray,
        cancel = colors.red,
        confirm = colors.green,
        number = colors.cyan,
        boolean = colors.cyan,
        strings = colors.red,
        val = colors.yellow,
        info = colors.magenta,
        dir = colors.blue,
    },
    start_programs = { ".spyglass/programs/files.lua" }
}